spring:
  application:
    name: server-app
  datasource:
    url: jdbc:h2:mem:test_db
  #    url: jdbc:mysql:///test_db
  #    username: root
  #    password: 123456
  mvc:
    servlet:
      path: /api
  sql:
    init:
      schema-locations:
        - classpath*:init_schema.sql
  h2:
    console:
      enabled: true
      path: /h2

  servlet:
    multipart:
      max-file-size: 150MB
      max-request-size: 150MB
project:
  file:
    save-avatar-path: D:\cyh_code\github\intelligent-rating-system\avatar/
